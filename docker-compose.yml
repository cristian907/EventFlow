services:
  postgresql_db:
    image: postgres:18.4
    container_name: postgresql_db
    env_file:
      - ./packages/api/.env
    ports:
      - "5432:5432"
    volumes:
      - postgresql_data:/var/lib/postgresql/data

volumes:
  postgresql_data:
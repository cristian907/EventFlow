services:
  postgresql_db:
    image: postgres:latest
    container_name: eventflow
    restart: always
    env_file:
      - ./packages/api/.env
    ports:
      - "5432:5432"
    volumes:
      - postgresql_data:/var/lib/postgresql/

volumes:
  postgresql_data: